import 'dart:convert';
import 'package:http/http.dart' as http;
import 'lib/api/token.dart';
import 'lib/endpoint/endpoint.dart';

void main() async {
  // Use a fake token or assume we don't have token? 
  // Wait, I can't just run dart since I don't have the real token.
}
