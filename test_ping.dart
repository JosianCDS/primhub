import 'package:http/http.dart' as http;
import 'dart:convert';

void main() async {
  final token = "test"; // I don't have a real token here.
  // Actually, I can't run dart scripts that easily if I don't have a valid token.
}
